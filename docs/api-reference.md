# Code reference

Auto-generated from the NumPy-style docstrings in `src/` via
[mkdocstrings](https://mkdocstrings.github.io/). For the *HTTP* endpoints, see the
[HTTP API](api.md) page instead.

## hid_keyboard_server

The HTTP bridge: turns requests into USB-HID reports, serves the web remote and
HLS segments, and relays RTSP to HLS/WebRTC.

::: hid_keyboard_server

## ir_tool

The Broadlink RM4 IR helper (discover / learn / send).

::: ir_tool
