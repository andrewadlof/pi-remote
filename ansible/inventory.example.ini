# Copy to inventory.ini and edit for your Pi.
# The user must be able to sudo (Raspberry Pi OS default user can).

[pi_remote]
pi-remote ansible_host=192.168.1.50

[pi_remote:vars]
ansible_user=pi
ansible_python_interpreter=/usr/bin/python3
